# Wave Android UI

Wave Android uses Chromium as the browser engine. The Wave UI is intentionally minimal for now:

- Address/search bar
- Bookmark star for the current page

The UI must drive Chromium's navigation and tab/page state; it must not use Android WebView as a replacement engine.

Future UI/features should be added only when intentionally requested.
