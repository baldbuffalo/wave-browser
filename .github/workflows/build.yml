name: Build Wave Browser (WiiU)

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: devkitpro/devkitppc:latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Install WiiU packages (NO -Sy)
      - name: Install WiiU toolchain
        run: |
          dkp-pacman -S --needed --noconfirm wiiu-dev wiiu-curl

      # Copy headers from installed wut
      - name: Copy wut headers
        run: |
          mkdir -p include
          cp -r $DEVKITPRO/wut/include/* include/

      - name: Build
        run: make

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: wave-browser-build
          path: |
            *.rpx
            *.elf
