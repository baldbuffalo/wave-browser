name: Build Wave Browser (WiiU)

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: devkitpro/devkitppc:latest

    env:
      DEVKITPRO: /opt/devkitpro
      DEVKITPPC: /opt/devkitpro/devkitPPC
      WUT_ROOT: /opt/devkitpro/wut
      PORTLIBS: /opt/devkitpro/portlibs/wiiu

    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.2

      - name: Install dependencies
        run: |
          apt-get update
          apt-get install -y zip libxml2-utils

      - name: Add devkitPro tools to PATH
        run: |
          echo "/opt/devkitpro/devkitPPC/bin" >> $GITHUB_PATH
          echo "/opt/devkitpro/tools/bin" >> $GITHUB_PATH

      - name: Check freetype
        run: |
          find /opt/devkitpro -name "libfreetype*"
          find /opt/devkitpro -name "freetype2" -type d

      - name: Download font
        run: |
          FONT_URL=$(curl -s -A "Mozilla/4.0" "https://fonts.googleapis.com/css?family=Roboto:700" | grep -o 'https://[^)]*' | head -1)
          wget -q "$FONT_URL" -O wave_browser/font.ttf
          xxd -i wave_browser/font.ttf > wave_browser/font_data.h
          sed -i 's/unsigned char wave_browser_font_ttf\[\]/static const unsigned char font_data[]/g' wave_browser/font_data.h
          sed -i 's/unsigned int wave_browser_font_ttf_len/static const unsigned int font_data_len/g' wave_browser/font_data.h

      - name: Build Wave Browser
        run: |
          make clean
          make -j$(nproc)

      - name: Validate meta.xml
        run: |
          if [ ! -f meta.xml ]; then
            echo "meta.xml missing!" && exit 1
          fi
          xmllint --noout meta.xml

      - name: Prepare release folder
        run: |
          mkdir -p WaveBrowser
          cp WaveBrowser.wuhb WaveBrowser/
          cp meta.xml WaveBrowser/

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: WaveBrowser
          path: WaveBrowser/
