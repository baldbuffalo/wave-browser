name: Build Wave Browser (WiiU)

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: devkitpro/devkitppc:6.0.2

    steps:
      # 1️⃣ Checkout repo
      - name: Checkout repository
        uses: actions/checkout@latest

      # 2️⃣ Install WiiU packages
      - name: Install WiiU packages
        run: |
          sudo dkp-pacman -Syu --noconfirm
          sudo dkp-pacman -S --noconfirm wiiu-dev wiiu-curl

      # 3️⃣ Build project
      - name: Build
        run: make

      # 4️⃣ Upload RPX / ELF artifact
      - name: Upload build artifact
        uses: actions/upload-artifact@6
        with:
          name: wave-browser-build
          path: |
            *.rpx
            *.elf
