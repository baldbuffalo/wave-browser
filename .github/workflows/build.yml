name: Build Wave Browser (WiiU)

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: devkitpro/devkitppc

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2
        with:
          fetch-depth: 1
          submodules: false

      - name: Set up DevkitPro
        run: |
          export DEVKITPRO=/opt/devkitpro
          export DEVKITPPC=$DEVKITPRO/devkitPPC
          export PATH=$DEVKITPPC/bin:$DEVKITPRO/tools/bin:$PATH

      - name: Build RPX
        run: |
          make clean
          make

      - name: Check meta.xml
        run: |
          if [ ! -f meta.xml ]; then
            echo "meta.xml missing!" && exit 1
          fi

      - name: Convert RPX → WUHB
        run: |
          # Convert using existing meta.xml
          wuhbtool build/wave_browser.rpx build/wave_browser.wuhb -m meta.xml

      - name: Prepare Aroma folder
        run: |
          mkdir -p release/WaveBrowser
          cp build/wave_browser.wuhb release/WaveBrowser/
          cp meta.xml release/WaveBrowser/

      - name: Zip WaveBrowser folder
        run: |
          cd release
          zip -r WaveBrowser.zip WaveBrowser
