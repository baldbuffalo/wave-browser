name: Build Wave Browser (WiiU)

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: devkitpro/devkitppc

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: Set up DevkitPro environment
        run: |
          export DEVKITPRO=/opt/devkitpro
          export DEVKITPPC=$DEVKITPRO/devkitPPC
          export WUT_ROOT=$DEVKITPRO/wut
          export PORTLIBS=$DEVKITPRO/portlibs/wiiu
          export PATH=$DEVKITPPC/bin:$DEVKITPRO/tools/bin:$PATH
          which powerpc-eabi-gcc
        shell: sh

      - name: Build RPX
        run: |
          make clean
          make
        shell: sh

      - name: Verify RPX and meta.xml
        run: |
          if [ ! -f build/wave_browser.rpx ]; then
            echo "wave_browser.rpx not found!" && exit 1
          fi
          if [ ! -f meta.xml ]; then
            echo "meta.xml missing!" && exit 1
          fi
        shell: sh

      - name: Convert RPX → WUHB
        run: |
          mkdir -p release/WaveBrowser
          wuhbtool build/wave_browser.rpx build/wave_browser.wuhb meta.xml
          cp build/wave_browser.wuhb release/WaveBrowser/
          cp meta.xml release/WaveBrowser/
        shell: sh

      - name: Create ZIP for Aroma
        run: |
          cd release
          zip -r WaveBrowser.zip WaveBrowser
        shell: sh

      - name: Upload WUHB zip
        uses: actions/upload-artifact@v7.0.0
        with:
          name: WaveBrowser
          path: release/WaveBrowser.zip
