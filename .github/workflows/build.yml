name: Build Wave Browser (WiiU)

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: devkitpro/devkitppc

    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Set up environment
        run: |
          export DEVKITPRO=/opt/devkitpro
          export DEVKITPPC=$DEVKITPRO/devkitPPC
          export PATH=$DEVKITPPC/bin:$DEVKITPRO/tools/bin:$PATH

      - name: Build RPX
        run: |
          make clean
          make

      - name: Convert RPX to WUHB
        run: |
          # Ensure meta.xml exists
          if [ ! -f meta.xml ]; then
            echo "meta.xml missing!" && exit 1
          fi

          # Convert RPX to WUHB using existing meta.xml
          wuhbtool build/wave_browser.rpx build/wave_browser.wuhb -x meta.xml

      - name: Prepare Aroma package
        run: |
          mkdir -p release/WaveBrowser
          cp build/wave_browser.wuhb release/WaveBrowser/

          cd release
          zip -r WaveBrowser.zip WaveBrowser

      - name: Upload Aroma package
        uses: actions/upload-artifact@v7.0.0
        with:
          name: WaveBrowser
          path: release/WaveBrowser.zip
